# XC7A35T / ACX720 倒立摆移植 TODO

> 目标器件：`xc7a35tfgg484-2`  
> 开发板：小梅哥 ACX720（硬件版本待确认）  
> STM32 参考工程：`D:\github\2-2-STM32\STM32\JX-Fork\JX-DaoLiBai\JX-DaoLiBai-Template`  
> 原则：每个模块先做独立 testbench，再做独立上板例程；验收通过后才接入闭环。

## 0. 已从 STM32 工程确认的控制基线

- [ ] 把以下“行为基线”抄入后续仿真说明，移植时以当前源码为准，不以旧审查文档为准。
  - 倒立摆核心位于 `Mode/Mode_5.c`，不是 Mode 2/3/4。
  - 每 `1 ms`：采集 12 位摆角 ADC，并做越界停机。
  - 每 `5 ms`：摆角内环 PID，输出有符号 PWM 命令 `-100..100`。
  - 每 `50 ms`：小车位置外环 PID，输出摆角目标偏置；函数虽叫 `Timer_20ms_Callback`，当前 `Counter=50`，实际不是 20 ms。
  - PWM 频率约 `20 kHz`。
  - 摆角中心 ADC 码为 `2060`，允许范围为严格的 `1260 < angle < 2860`，等于边界也停机。
  - 编码器换算参数为 `PPR=11`、减速比 `9.27666`、四倍频。
- [ ] 记录当前 PID 参数，先复刻行为，再进行 FPGA 实机重调。
  - 内环：`Kp=0.23`、`Ki=3.08`、`Kd=0.00943`、`dt=0.005 s`、积分状态限幅 `±5`、输出限幅 `±100`。
  - 外环：`Kp=0.622`、`Ki=0.152`、`Kd=0.345`、`dt=0.050 s`、积分状态限幅 `±50`、输出限幅 `±100`。
  - 外环目标位置为 `0°`；内环目标为 `2060 - outer_output`。
- [ ] 明确调参笔记中的 `100 ms` 是后续推荐实验值，而当前源码真实运行值是 `50 ms`；先用 50 ms 做黄金模型一致性测试，再把 50/100 ms 做成可配置参数进行实机比较。

## 1. 先冻结硬件接口（未完成前不要连接电机功率级）

- [ ] 确认手中 ACX720 的准确硬件版本（老款/V3/其他版本），保存对应用户手册、原理图和官方管脚约束文件。
- [ ] 从官方资料核对板载系统时钟频率；若为常见的 `50 MHz`，记录 `F_CLK=50_000_000`，否则所有分频参数随实际值修改。
- [ ] 在新建 XDC 前核对 `xc7a35tfgg484-2`、管脚号、Bank 电压和 `IOSTANDARD`，禁止直接照抄 STM32 的 PA/PB 引脚名。
- [ ] 确认电机驱动芯片型号、逻辑电压、真值表、PWM 接法、刹车/滑行语义、最大 PWM 频率以及方向切换是否要求死区。
- [ ] 确认编码器 A/B 相输出电压、是否开漏、是否需要上拉、最大边沿频率、每电机轴/输出轴实际脉冲数和机械方向。
- [ ] 确认摆角传感器类型、输出电压范围、中心电压、噪声幅度和机械有效角范围。
- [ ] 决定摆角 ADC 接入方案，这是 PID 移植前的硬门槛。
  - 方案 A：若该版本 ACX720 已有可接外部信号的 ADC，确认芯片型号、接口和输入范围。
  - 方案 B：使用外置 SPI ADC，优先选择采样率明显高于 1 kS/s、输入范围匹配且有现成时序资料的器件。
  - 方案 C：第一阶段保留 STM32 采 ADC，通过 SPI/UART 向 FPGA 发 12 位样本，只用于快速验证 FPGA 控制链；最终再替换为独立 ADC。
  - 不要把模拟信号直接接普通 FPGA GPIO；只有确认专用 XADC 引脚已正确引出且满足电气规范时才能考虑 XADC。
- [ ] 画出并复核最终连接表：`FPGA pin / Bank / IOSTANDARD / 外设信号 / 外设电压 / 默认安全电平`。
- [ ] 确认 FPGA 与电机驱动、ADC、编码器共地，并确认任何外部 5 V 信号已经做电平转换，不能直接进入 3.3 V FPGA Bank。
- [ ] 设置硬件急停或驱动 EN 默认关断；FPGA 未配置、复位中或程序异常时，电机必须保持无动力。

**阶段门槛：** 原理图版本、ADC 方案、供电/电平、H 桥真值表和最终引脚表均已确认。

## 2. 建立 Vivado 工程骨架

- [ ] 在 `Vivado_Test` 下为倒立摆新建独立工程，不覆盖现有 `Vivado_Test/GPIO`，器件选择 `xc7a35tfgg484-2`。
- [ ] 建立目录：`rtl/`、`tb/`、`xdc/`、`sim_vectors/`、`docs/`、`scripts/`。
- [ ] 规定所有控制逻辑只使用一个系统时钟；1/5/50 ms 和 20 kHz 均生成单周期 clock-enable，不派生新的逻辑时钟。
- [ ] 建立统一复位模块：外部复位异步触发、同步释放；所有执行器寄存器复位值必须为 PWM=0、方向安全、未使能。
- [ ] 建立参数包/头文件，集中定义 `F_CLK`、PWM 频率、PID 周期、位宽、ADC 中心/边界和编码器参数，禁止散落 magic number。
- [ ] 给每个 RTL 模块启用 ``default_nettype none`` 或等价的严格连线检查，并在文件尾恢复设置（如工程规范要求）。
- [ ] 建立最小自动验证流程：行为仿真通过、综合无 latch/多驱动、实现无严重 DRC、时序满足、上板验收记录齐全。

## 3. 例程 1：时钟、复位与 GPIO

- [ ] 编写 `reset_sync`，验证复位期间所有电机相关输出为安全值。
- [ ] 编写参数化 `tick_gen`，先只产生 LED 闪烁用 clock-enable。
- [ ] 编写 GPIO 例程：拨码/按键经过 2 级同步后控制 LED，不直接把异步输入用于状态机。
- [ ] 若使用机械按键作为使能，加入 10～20 ms 去抖；第一版只需“按下/释放或拨码电平”，暂不做单击、双击、长按完整状态机。
- [ ] 编写 testbench，覆盖上电复位、复位抖动、异步输入靠近时钟边沿和连续按键抖动。
- [ ] 上板验证 LED 周期和使能输入；用 ILA 确认复位同步释放、去抖输出只变化一次。

**验收：** 任意复位或失能状态都能在逻辑上强制电机 PWM=0，且异步 GPIO 不直接进入控制数据通路。

## 4. 例程 2：20 kHz PWM

- [ ] 编写参数化边沿对齐 PWM：`F_PWM=20_000 Hz`，输入占空比统一为 `0..100` 或更高分辨率内部表示。
- [ ] 明确定义 100% 占空比，不复刻 STM32 `ARR=99` 却允许 compare=100 的模糊边界。
- [ ] 占空比只在周期边界装载，避免运行中修改比较值产生窄脉冲。
- [ ] 加入 `enable`/`fault` 最高优先级关断，关断时 PWM 输出必须为 0。
- [ ] 编写 testbench，覆盖 0%、1%、25%、50%、99%、100%、周期边界更新和异步故障关断。
- [ ] 上板用示波器/逻辑分析仪测量频率、占空比和更新毛刺；保存 0/25/50/75/100% 波形截图。

**验收：** 频率误差符合分频设计，五档占空比正确，0/100% 定义清晰，更新无毛刺，故障可强制拉低。

## 5. 例程 3：H 桥方向与开环电机

- [ ] 编写 `signed_motor_cmd`：先在宽位有符号域饱和到 `-100..100`，再取方向和绝对值，避免 STM32 `float → int8_t` 先窄化再限幅的问题。
- [ ] 保持与 STM32 初始方向约定一致：正值对应 `AIN1=0, AIN2=1`，负值相反；若实物方向不一致，只在一个参数中翻转。
- [ ] 根据驱动数据手册实现换向保护：先 PWM=0，再等待规定死区，最后切方向并恢复 PWM。
- [ ] 定义 `0` 命令、DISABLED、FAULT、RESET 分别输出刹车还是滑行，并写入接口说明。
- [ ] 编写 testbench，覆盖正转→停→反转、反转→正转、饱和、使能撤销和故障中途到来。
- [ ] 首次上电不装摆杆/架空小车轮，使用限流电源和很小占空比测试正转、停机、反转。
- [ ] 验证 FPGA 掉复位、重新配置和拔掉时钟时，外部硬件仍不会让电机失控。

**验收：** 方向正确、换向无直通风险、任何未使能/故障/复位状态都立即或按硬件安全要求进入零输出。

## 6. 例程 4：UART TX 遥测（强烈建议，非闭环必需）

- [ ] 编写 `uart_tx`，先固定 `115200 8N1`，周期发送计数值或紧凑二进制帧。
- [ ] 加小 FIFO 或发送握手，调试输出不得阻塞 1/5/50 ms 控制逻辑。
- [ ] 编写 testbench，逐位检查起始位、8 数据位、停止位和连续字节间隔。
- [ ] 上位机连续接收至少 10 分钟，确认无乱码、丢字和异常停顿。
- [ ] 预留遥测字段：时间戳、ADC、编码器累计、内外环误差/P/I/D/输出、PWM、enable、fault code。

**验收：** UART 仅作为旁路观察者，拔掉串口或 FIFO 满都不能改变控制时序和电机安全状态。

## 7. 例程 5：正交编码器

- [ ] 编写 `quadrature_decoder`：A/B 各做 2 级同步，使用合法 Gray 状态转移表进行 x4 解码。
- [ ] 累计位置至少使用有符号 32 位；不要复刻 STM32 `int32_t` 累计却以 `int16_t` 返回的截断问题。
- [ ] 输出 `delta_count`、`position_count`、`illegal_transition` 和可选样本快照有效信号。
- [ ] 根据实测噪声决定是否加入 N 拍稳定滤波；滤波不能吞掉最高转速下的合法边沿。
- [ ] 编写 testbench：`00→01→11→10→00` 为约定正向 +4，反序为 -4；覆盖抖动、非法 `00→11`、停在边沿和 32 位符号跨越附近。
- [ ] 用信号源先注入已知 A/B 相，再接真实编码器手转；UART/ILA 对照圈数和方向。
- [ ] 计算并核对位置换算：`position_deg = count × 360 / (4 × 11 × 9.27666)`；RTL 中优先使用定点比例常数，避免运行时除法器。

**验收：** 正反一圈计数、方向和累计值可重复；非法跳变可观测；长时间计数不会在 16 位处回绕。

## 8. 例程 6：摆角 ADC 接口

- [ ] 按第 1 阶段选定的器件编写 ADC 接口；若为 SPI，参数化 CPOL/CPHA、SCLK、CS 建立/保持时间和数据位数。
- [ ] 输出统一接口：`adc_angle[11:0]`（不足/超过 12 位时明确缩放）、`adc_valid`、`adc_error`、`sample_timestamp`。
- [ ] 保证至少每 1 ms 得到一个新样本；控制环只消费完整样本，绝不消费移位过程中的半帧数据。
- [ ] 编写 ADC 行为模型和 testbench，覆盖 0、1260、1261、2060、2859、2860、4095、超时、帧错误和接口断线。
- [ ] 上板用已知直流电压核对零点、中点、满量程与线性；再接摆角传感器测中心值和噪声。
- [ ] 不直接假定 STM32 的中心 `2060` 可原样使用；完成机械竖直标定，记录 FPGA ADC 实际中心 `ANGLE_CENTER`。
- [ ] 测量静止噪声与快速摆动时的码率，确定是否需要中值/滑动平均/一阶低通；任何滤波都要量化相位延迟。

**验收：** ADC 值、valid、错误和超时语义稳定；竖直中心可重复；1 kHz 更新无漏样；断线能可靠触发故障。

## 9. 例程 7：1/5/50 ms 调度器

- [ ] 由系统时钟生成单周期 `tick_1ms`，再计数生成 `tick_5ms` 和可配置的 `tick_outer`（初始 50 ms，可切换 100 ms）。
- [ ] PWM 使用独立 20 kHz clock-enable，但仍处于同一系统时钟域。
- [ ] 对所有整数分频添加 elaboration/仿真断言；若系统时钟不能整除，采用误差累加/Bresenham 分频并计算长期误差。
- [ ] 定义同一拍 tick 碰撞时的行为以复刻 STM32：先锁存 1 ms ADC/保护；5 ms 内环本次使用旧外环目标；外环的新目标从下一个 5 ms 内环开始生效。
- [ ] 编写至少 2 秒仿真，自动统计各 tick 周期、脉宽、相位关系和长期累计个数。
- [ ] 用 ILA 检查 1/5/50 ms 都是单周期 enable，且长期无漂移。

**验收：** 全设计只有一个主时钟；控制 tick 频率、顺序和更新语义与说明一致。

## 10. 例程 8：安全监督状态机

- [ ] 实现 `RESET → DISABLED → ARMED → FAULT` 状态机，上电默认 DISABLED，必须明确操作才能 ARMED。
- [ ] 每 1 ms 检查摆角范围；初始兼容 STM32 的严格条件 `1260 < angle < 2860`，后续用标定值参数化。
- [ ] 将 `!adc_valid`、ADC 超时、ADC 错误、角度越界、急停、复位、编码器非法率过高纳入故障源。
- [ ] FAULT 组合/时序路径拥有最高优先级，强制 PWM=0，不能等待下一个 5 ms PID tick。
- [ ] 明确故障锁存与恢复策略：建议倾倒/传感器异常锁存，PWM 保持 0，只有失能后重新确认才能清除。
- [ ] 失能或故障时清零两环积分、历史误差和输出，避免重新使能时产生“踢变”；这是对 STM32 版本的有意安全改进。
- [ ] 编写 testbench 覆盖：1260/2860/4095 均故障，1261/2859 范围有效；enable=0 始终 PWM=0；运行中断 ADC、越界、复位均关断。

**验收：** 所有故障注入都得到确定、可锁存、可解释的 fault code，且电机输出优先归零。

## 11. 例程 9：定点 PID 核

- [ ] 先在 PC 建立与 `Software/MyPID.c` 行为一致的黄金模型，保存输入、误差、积分、P/I/D 和限幅后输出为 CSV/HEX 测试向量。
- [ ] 决定定点格式并写设计说明；可先评估：状态/输入 `signed 25-bit Q17.8`、系数 `signed 18-bit Q4.14`、48 位乘加累加器。
- [ ] 使用预折叠系数减少运行时除法：`KiTs=Ki×dt`、`KdOverTs=Kd/dt`。
- [ ] 若采用 Q4.14，先验证下列量化系数：
  - 内环：`Kp=3768`、`KiTs=252`、`KdOverTs=30900`。
  - 外环 50 ms：`Kp=10191`、`KiTs=125`、`KdOverTs=113050`。
  - 外环 100 ms：`KiTs=249`、`KdOverTs=56525`，其余不变。
- [ ] 编写可复用多周期 PID FSM：`LATCH → ERR/DELTA → SUM+SAT → P/I/D MAC → ROUND/SAT → COMMIT`；只在 COMMIT 更新历史状态。
- [ ] 明确每一步的位宽增长、符号扩展、乘积小数位、舍入方式、积分饱和和输出饱和，禁止依赖 Verilog 隐式截断。
- [ ] 内环按“采样积分和”实现时，把 `SumError` 等价限幅换算为 `±1000` 个 ADC 码样本；外环 50 ms 同样为 `±1000` 个角度样本，100 ms 时为 `±500`。
- [ ] 若内外环共享一个 DSP48E1，给调度器增加 `start/busy/done` 握手，并断言最坏延迟远小于 5 ms。
- [ ] 用以下内环连续向量验证复位后输出：`actual=[2060,2050,2050,2070]`；浮点参考约 `[0,21.314,2.608,-39.866]`；Q8 参考 `[0,5456,667,-10206]`。
- [ ] 用以下外环 50 ms 连续向量验证：`actual_deg=[0,10,10,100]`；浮点参考约 `[0,-75.296,-6.372,-100]`；Q8 参考 `[0,-19276,-1632,-25600]`。
- [ ] 另测零误差、正负阶跃、积分饱和、输出饱和、符号边界、复位/失能中断一次计算以及连续一万拍无溢出。
- [ ] 定义 FPGA 对浮点黄金模型的允许误差，并确保误差主要来自已记录的系数量化/舍入，不来自位宽溢出。

**验收：** 每个状态量逐拍可解释；所有黄金向量、边界向量和随机回归通过；综合资源和时序满足。

## 12. 例程 10：只闭合摆角内环

- [ ] 顶层连接：ADC → 安全状态机 → 5 ms 内环 PID → 有符号 PWM/H 桥；暂时固定外环偏置为 0，即目标为标定中心。
- [ ] 先在仿真中注入中心附近正负摆角，确认“摆杆向一侧偏时，小车必须向能托住摆杆的方向运动”；方向错时用统一参数翻转，不能到处改负号。
- [ ] 在电机断开时用 ADC 实物摆动和 UART/ILA 检查误差、PID 输出、饱和和越界关断。
- [ ] 在架空/限流条件下接电机，先把输出上限临时限制到很小值，确认误差方向与电机方向闭环符号正确。
- [ ] 机械装配后由人扶住摆杆，逐步提高 PWM 限幅；每次只改一个参数并保存波形。
- [ ] 验证失能、放倒、拔 ADC、复位均立即停机；确认重新使能不会继承旧积分。

**验收：** 不接外环时能表现出正确的扶正趋势，且所有安全路径通过；未确认反馈符号前禁止满功率。

## 13. 例程 11：加入小车位置外环

- [ ] 将编码器 32 位累计位置快照送入 50 ms 外环 PID，先用定点比例换算为角度或直接把 PID 参数换算到 count 域，两者只能选一种明确量纲。
- [ ] 外环输出限幅到 `±100` ADC 码偏置，生成 `inner_goal = ANGLE_CENTER - outer_output`。
- [ ] 保持 STM32 调用语义：碰到 50 ms 与 5 ms 同拍时，当前内环先用旧目标，外环新目标下一次 5 ms 生效。
- [ ] 仿真验证小车位移正/负时摆角目标偏置方向正确，并覆盖外环输出饱和。
- [ ] 上板先用 50 ms 复刻当前 STM32 参数；完成可重复测试后再切到调参笔记推荐的 100 ms，分别记录抖动、恢复时间和最大位移。
- [ ] 添加软行程边界；若小车位置超限，禁止位置环继续把车推向边缘并进入受控停机/故障。

**验收：** 摆杆可稳定时，小车能回到参考位置附近；50/100 ms 差异有数据记录；行程边界有效。

## 14. 完整顶层集成与验收

- [ ] 建立最简顶层数据流：`clock/reset → ADC/QDEC → tick scheduler → two-rate PID → safety → signed PWM/dir`。
- [ ] 加 UART TX 或 ILA 旁路观测，保证调试模块移除后控制结果不变。
- [ ] 编写系统级 testbench，使用录制的 STM32 ADC/编码器序列回放，对比 FPGA 内外环和 PWM 输出。
- [ ] 对跨模块接口添加 assertion：样本只能在 valid 时锁存、PID busy 时不能重启、fault 时 PWM 必为 0、占空比永不超过上限。
- [ ] 完成综合/实现，检查 LUT/FF/BRAM/DSP 使用量、所有警告、CDC 报告和最差时序裕量。
- [ ] 上电顺序验收：上电默认不转 → 传感器正常 → 人工扶正 → 明确使能 → 才允许输出。
- [ ] 故障验收：倾倒、ADC 断线/超时、编码器异常、行程越界、急停、复位、重新配置，逐项验证 PWM 归零。
- [ ] 性能验收：记录平衡持续时间、最大摆角、小车位置范围、恢复时间、PWM 峰值、ADC 噪声和控制周期抖动。
- [ ] 保存可复现实验条件：机械参数、供电电压、电机驱动型号、ADC 型号、中心码、PID 参数、位宽、bitstream 提交号。

## 15. 闭环稳定后再做的功能

- [ ] 实现 UART RX 在线调参；采用带帧头、长度、校验和、范围检查的流式协议，完整帧校验通过后原子更新参数。
- [ ] 给参数更新增加安全策略：仅 DISABLED 时允许改关键量，或在更新后清 PID 状态并重新确认使能。
- [ ] 若确有需要，再移植复杂按键单击/双击/长按状态机。
- [ ] 若确有需要，再实现 OLED；显示刷新必须低优先级，不能改变控制 tick。
- [ ] 若确有需要，再实现四路 RP 电位器 ADC 调参；它不属于 Mode 5 最小闭环。
- [ ] 评估 MicroBlaze 只承载 UI/通信、RTL 承载硬实时控制的混合架构；首版纯 RTL 闭环不依赖它。

## 16. 明确不需要原样移植的 STM32 内容

- [ ] 不移植 CubeMX/HAL/CMSIS、NVIC、中断向量、DMA 句柄和 STM32 时钟初始化代码。
- [ ] 不移植 `Top/Mymain.c` 的超级循环和完整 Mode 1～10 分发框架；FPGA 用并行数据通路、clock-enable 和安全 FSM 表达行为。
- [ ] 不移植 DWT `Timer_Counter`；使用 FPGA 自由运行计数器、仿真时间和 ILA 测量延迟。
- [ ] 不把 `MyPID.c` 浮点 C 代码逐句翻译成 RTL；只复刻公式、状态更新顺序、限幅和参数，并显式定点化。
- [ ] 不在最小闭环阶段移植 OLED、字库、ADC2/RP、UART DMA、ABC/HEX 完整协议和 Mode 6～10 空壳。

## 17. 关键源码复查索引

- [ ] 调度复查：`Mode/Mode_G.c:68-104`、`Core/Src/stm32f1xx_it.c:186-195`。
- [ ] 倒立摆控制复查：`Mode/Mode_5.c:3-95`。
- [ ] PID 行为复查：`Software/MyPID.c:30-79`、`Software/MyPID.h:6-37`。
- [ ] PWM/电机方向复查：`Core/Src/tim.c:45-81`、`Hardware/PWM.c:4-14`、`Hardware/Motor.c:21-45`。
- [ ] 编码器复查：`Core/Src/tim.c:98-165`、`Hardware/Encoder.c:4-23`。
- [ ] 摆角 ADC 复查：`Core/Src/adc.c:31-71,119-138`、`Hardware/AD.c:3-27`。
- [ ] 引脚/辅助接口复查：`Core/Inc/main.h:60-77`、`Core/Src/gpio.c:42-90`、`Core/Src/usart.c:33-123`。

## 最短执行路径

- [ ] 严格按以下顺序推进并逐项打勾：`硬件冻结 → GPIO → PWM → H桥开环 → UART TX → 编码器 → ADC → 调度器 → 安全FSM → 定点PID → 摆角内环 → 位置外环 → 完整验收`。
- [ ] 任一阶段未通过 testbench 和上板验收时，不进入下一阶段，尤其不能带着未确认的反馈方向直接闭环上电。
